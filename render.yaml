services:
  - type: web
    name: datawave-engine
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn datawave.main:app --host 0.0.0.0 --port $PORT
    plan: free
